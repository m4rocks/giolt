---
import type { SelectBlogPosts, SelectOrganizations } from "@/db/schema";
import { mdClassName } from "@/lib/markdown";
import BlogWriter from "../blog-writer.astro";

interface Props {
	orgId: string;
	post: SelectBlogPosts;
}

const { post, orgId } = Astro.props;
---

<div class="container">
	<h1
		class="text-5xl md:text-7xl font:bold"
	>
		{post.title}
	</h1>
	<p
		class="text-base-content/75 mb-2"
	>
		{post.description}
	</p>
	<BlogWriter
		orgId={orgId}
		writerId={post.writerId}
	/>
	<div
		set:html={post.content}
		class={`${mdClassName} mt-4`}
	></div>
</div>
